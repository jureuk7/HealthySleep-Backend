let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_d80‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_d80‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_d80‍.w("koa-bodyParser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_d80‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_d80‍.w("./api",[["default",["api"],function(v){api=v}]]);_d80‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require("dotenv").config();








// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 만들기
const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, {
    useNewUrlParser: true,
  })
  .then(() => {
    console.log("Connected to Mongodb");
  })
  .catch((e) => {
    _d80‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use("/api", api.routes());

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// PORT가 지정되어 있지 않다면 4000을 사용
const port = PORT || 4000;
app.listen(port, () => {
  _d80‍.g.console.log("Listening to port %d", port);
});
